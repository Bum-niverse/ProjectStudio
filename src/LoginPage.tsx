interface Props{message?:string;isAuthenticating:boolean;onLogin:()=>void;onStartLogin:()=>void}
export function LoginPage({message,isAuthenticating,onLogin,onStartLogin}:Props){return <main className="login-page"><section className="login-card"><div className="login-brand"><span>PS</span><div><p className="eyebrow">LOCAL PRODUCT WORKSPACE</p><h1>ProjectStudio</h1></div></div><div className="login-copy"><p className="eyebrow">PRIVATE WORKSPACE</p><h2>GitHub 계정으로 작업대를 잠금 해제합니다.</h2><p>이 컴퓨터의 GitHub CLI 인증을 확인한 뒤 소유자 계정에서만 로컬 기획 문서를 불러옵니다.</p></div><div className="login-security"><span>✓ 로그인 전 프로젝트 데이터 미조회</span><span>✓ 토큰을 ProjectStudio에 저장하지 않음</span><span>✓ 소유자 계정만 개발자 모드 허용</span></div>{message&&<p className="login-message">{message}</p>}<button className="github-login-button" disabled={isAuthenticating} onClick={onLogin} type="button"><span aria-hidden="true">●</span>{isAuthenticating?"GitHub 계정 확인 중…":"GitHub로 로그인"}</button><button className="github-auth-start secondary" onClick={onStartLogin} type="button">GitHub CLI 로그인이 필요한 경우</button><small>로그인 화면은 앱 진입을 제한합니다. 로컬 파일 암호화는 Windows 계정 보안의 적용을 받습니다.</small></section></main>}
