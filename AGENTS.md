# ProjectStudio 개발 지침

## 프로젝트 정의

ProjectStudio는 개인이 여러 소프트웨어 프로젝트를 기획하고 개발 완료까지 추적하는 로컬 우선 데스크톱 프로그램이다. AI 채팅, PRD, 기능명세, 유저플로우, 시스템 설계, 품질 검사, 변경 기록, 내보내기, AI 변경안 승인·거절 흐름을 구현하고 코드·커밋·테스트·배포 추적 기능으로 확장한다.

Manyfast의 UI, 문구, 브랜드와 구현을 그대로 복제하지 않는다. 문제 해결 방식과 정보 구조를 참고하되 ProjectStudio만의 디자인과 코드 추적 경험을 만든다.

모든 사용자 설명, 문서와 작업 보고는 한국어로 작성한다. 코드 식별자와 표준 기술 용어는 영어를 사용한다.

## 제품 원칙

- 개인용 로컬 프로그램을 우선하며 로그인, 결제와 팀 기능을 초기 범위에서 제외한다.
- 프로젝트 데이터는 기본적으로 사용자 컴퓨터에 저장한다.
- AI가 제안한 변경은 사용자가 승인하기 전 원본 문서에 확정 반영하지 않는다.
- 외부 프로젝트 파일과 Git 저장소는 최소 권한으로 읽고, 쓰기 작업은 명시적으로 보여준다.
- 실제 프로젝트를 검증 사례로 사용하되 특정 프로젝트 이름이나 도메인에 종속된 생성 규칙을 두지 않는다.
- 기획 문서와 실제 구현 상태 사이의 추적 가능성을 핵심 가치로 둔다.

## 초기 기술 방향

- TypeScript strict mode
- React 기반 UI
- Tauri 데스크톱 셸
- SQLite 로컬 데이터베이스
- Markdown + JSON 내보내기
- React Flow 계열의 노드·엣지 시각화 검토
- Monaco Editor 계열의 문서·코드 비교 UI 검토
- Git CLI 연동은 별도 어댑터로 격리

새 프레임워크나 라이브러리 설치, 유료 AI/API 도입과 외부 전송이 발생하는 기능은 구현 전에 사용자에게 알린다.

## 핵심 기능

1. 프로젝트 생성과 로컬 저장
2. AI 채팅과 파일 첨부
3. 구조화된 PRD 생성·편집
4. 계층형 기능명세, 상태·중요도·역할·수용 기준
5. 유저플로우 생성·편집
6. 품질 검사와 구현 추적
7. 코멘트와 변경 이력
8. AI 변경안 비교·승인·거절
9. Markdown/JSON 내보내기
10. 기능과 코드 파일·브랜치·커밋·테스트 연결
11. 기획 변경 영향 분석과 개발 완료 추적

## 초기 제외 범위

- 다중 사용자 실시간 공동 편집
- 공개 공유 링크와 SaaS 호스팅
- 결제, 크레딧과 조직 권한
- 모바일 앱
- 사용자가 승인하지 않은 자동 코드 수정·커밋·푸시

## 코드 규칙

UI/UX를 변경하기 전에는 `docs/ui-ux-guidelines.md`를 함께 확인하고 버튼 계층, 테마 토큰, 접근성, 창 크기 대응과 작업 결과 보고 원칙을 따른다.

- 특별한 이유 없이 `any`, 타입 무시 지시문과 문제를 숨기는 강제 캐스팅을 사용하지 않는다.
- 변수와 함수는 camelCase, React 컴포넌트와 타입은 PascalCase, 상수는 UPPER_SNAKE_CASE를 사용한다.
- Boolean은 `is`, `has`, `can`, `should`, 이벤트 핸들러는 `handle`, 이벤트 속성은 `on`으로 시작한다.
- UI, 도메인, 저장소, AI, 파일 시스템과 Git 연동을 분리한다.
- Tauri 명령과 운영체제 접근을 UI 컴포넌트에서 직접 호출하지 않는다.
- 하나의 함수·파일·컴포넌트는 하나의 주요 책임을 갖는다.
- 주석은 코드 동작의 반복보다 설계 이유, 데이터 안전성과 외부 도구 제약을 설명한다.

## 데이터와 보안

- API 키, 토큰, 사용자 프로젝트 내용과 로컬 경로를 로그나 저장소에 커밋하지 않는다.
- 데이터베이스 변경에는 마이그레이션과 복구 방안을 제공한다.
- 외부 파일을 수정하기 전에 대상 경로와 변경 내용을 검증한다.
- Git 명령은 인자 배열 또는 안전한 실행 계층을 사용하고 사용자 입력을 셸 문자열로 직접 연결하지 않는다.
- AI로 전송할 문서와 코드 범위를 사용자가 알 수 있게 한다.
- 변경 이력은 원본 복구가 가능한 형태로 보존한다.

## 작업 절차

- 작업 전 저장소, 문서, 현재 Git 변경과 관련 테스트를 확인한다.
- 기능 목적, 사용자 흐름, 데이터 모델, 보안 영향과 완료 조건을 정리한다.
- 가장 작은 수직 기능 단위로 구현한다.
- 로딩, 빈 상태, 오류와 복구 흐름을 포함한다.
- 단위·통합·필요한 E2E 테스트, 타입 검사와 린트를 수행한다.
- 기능 변경 시 PRD, 기능명세와 진행 상태 문서를 함께 갱신한다.
- 검증된 변경은 의도한 파일만 커밋하고 GitHub에 푸시한다.

## 첫 수직 기능

사용자가 로컬 프로젝트를 만들고 아이디어를 입력하면 구조화된 PRD 초안을 생성·편집하고, 저장한 뒤 프로그램을 다시 열어 같은 PRD를 조회할 수 있게 한다. AI 공급자가 아직 연결되지 않은 단계에서는 교체 가능한 인터페이스와 명시적인 개발 모드만 허용하며, 동작하지 않는 목업을 완료로 표시하지 않는다.

## 프로젝트 하네스

- 비단순 작업은 `.agents/skills/`의 `repository-inspect → task-plan → validate-task → review-diff` 흐름을 적용한다.
- 인증·권한·개인정보·외부 도구 변경에는 `security-review`, UI 변경에는 `frontend-ui-review`, 데이터 계약 변경에는 `data-quality-review`를 추가한다.
- 전체 정책은 `docs/agent-guidelines/security.md`와 `docs/agent-guidelines/ui-ux.md`를 따른다. ProjectStudio 고유 규칙과 충돌하면 이 `AGENTS.md`가 우선한다.
- 공식 통합 검증 진입점은 `python scripts/validate.py`이며 실제 명령은 `.codex/validation-commands.json`에 인자 배열로 관리한다.
- 기본 검증은 `pnpm lint`, `pnpm test`, `pnpm build`, `cargo fmt --check --manifest-path src-tauri/Cargo.toml`, `cargo test --manifest-path src-tauri/Cargo.toml`이다.
- SQLite migration은 기존 데이터 보존과 롤백 가능성을 검토하고, Tauri command는 외부 입력 검증과 프로젝트 소유권을 Rust 계층에서 강제한다.
- `.projectstudio` 동기화와 내보내기는 사용자가 지정한 로컬 경로 안에서만 수행하고 비밀정보·토큰을 산출물이나 검증 보고서에 기록하지 않는다.
